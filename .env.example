# Copy to .env and fill in. .env is gitignored - never commit the real key.
# Klaviyo > Settings > API keys > Private API key (scopes: profiles write, lists write, templates read).
KLAVIYO_API_KEY=pk_replace_me

# List "Newsletter (double opt-in)", double opt-in enabled, created 2026-08-28.
KLAVIYO_LIST_ID=WZDGDT
